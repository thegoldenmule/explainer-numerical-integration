// Stub: the left interactive for this panel is not built yet; the prose fragment is.
export function mount(root) { root.append(Object.assign(document.createElement('p'), { className: 'muted', textContent: 'Interactive not built yet.' })); return { destroy() {} }; }
